import React from 'react'

function Green({props}) {
  return (
    <div>
        <button id='green' className='text-black fw-semibold rounded-5 border-0'>{props}</button>
    </div>
  )
}

export default Green

